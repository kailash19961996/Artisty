version: 1
applications:
  - appRoot: Agent-shopper/Agent-shopper/artisty-frontend
    frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    runtime-versions:
      nodejs: 20

